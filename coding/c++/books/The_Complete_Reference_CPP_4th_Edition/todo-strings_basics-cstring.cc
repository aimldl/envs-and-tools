// strings_basics-cstring.cc
//
// C-string or C-style string
//  is a char array or an array of characters terminated by the null character '\0',
//  i.e. C-string = char array + '\0'
//
// "#include <cstring>" or "#include <string.h>"
// C-string includes <cstring>, not <string>. <string.h> is also fine,
// but <string.h> is C syntax while <cstring> is C++ syntax.
// Note <string.h> is different from <string>, but the same as <cstring>.
//   <cstring>  == <string.h>
//   <string.h> != <string>
