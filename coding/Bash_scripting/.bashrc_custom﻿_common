# .bashrc_custom_common

############################################
#  Bash Shell Run Commands Customization   #
############################################
# Rev.3: 2019-12-18 (Wed)
# Rev.2: 2019-11-28 (Wed)
# Rev.1: 2019-10-23 (Wed)
# Draft: 2017-11-03 (Fri)
# Written by Tae-Hyung "T" Kim, Ph.D
#
# Usage
#   1. Open .bashrc, add the following line at the bottom, and save the file.
#      source .bashrc_custom_common
#   2. Type in the following command to apply the change in the .bashrc.
#      $ source .bashrc
# Troubleshooting
#   * Problem
#   $ source .bashrc
#   -bash: .bashrc_custom_common: No such file or directory
#   $
#   * Solution
#   Check if the file ".bashrc_custom_common" exists or the file name is correct.

##################
#  Shell Output  #
##################
# For example,
#   2017-11-03 Fri 09:59 (44th week)
export HISTIGNORE='ls:l:pwd:history:h'
echo `date +%F" ("%a") "%H:%M" ("%U"th week)"`  # Display time information

#################
#    History    #
#################
# To clear the previous history, type in:
#   $ history -c
# To recall previous command, type in:
#   $ !8
#   $ !net
export HISTCONTROL=erasedups                    # Erase dup(licate)s
# Add time format as follows:
#   273  2017-11-03 Fri 09:52:01 ls
export HISTTIMEFORMAT="%F %a %T "
HISTSIZE=10000                                  # Default size: 1000
HISTFILESIZE=20000                              # Default size: 2000

#############################
#  A Safe "delete" Command  #
#############################
# Disable the rm command and define a safe "delete" command.
# The delete command forcefully moves all specified files to directory .trash.
# Directory .trash works exactly same as Ubuntu GUI's "Trash" and Windows "Trash".
#
# Caution: Empty the .trash directory every now and then.
# TODO:
#   Create a house-keeping function to delete all trash files more than 30 days old.
#   The house-keeping function might also monitor how large the trash directory is getting.
#   If too many files are being stored, perhaps some need to be deleted to free up disk space.
# Source: pp.492~495, Unix Shell Programming Tools, David Medinets, McGraw-Hill

# Create a rm function that does nothing.
# This should help users keep out of mischief.
rm () {
  echo "Please use the delete command."
}

delete () {
  # Create directory ~/.trash if it doesn't exist.
  if [ ! -d ~/.trash ]; then
    mkdir ~/.trash
  fi

  if [ $# eq 0 ]; then
    # Warning message if there is no input argument.
    echo "Please specify a filename to delete."
  else
    echo "You are about to delete these files: $@"
    echo -n "Are you sure about this? Enter 'y' for yes."
    read
    # If 'y', force to move all the files to directory .trash.
    if [ "$REPLY" = "y" ]; then
      mv --force $@ ~/.trash
    fi
  fi
}

#################
#     Alias     #
#################
# *i is reserved for install
# e.g.
#   ai = apt-get install
#   pi = pip install
#   yi = yum install

alias a='conda activate hula'  # Activate Conda virtual environment "hula"
alias ai='sudo apt-get install -y'
#alias ap='sudo apt-get purge'
#alias b=''
alias c='cd'
alias d='~/.dropbox-dist/dropboxd'
# More alias for Docker
#alias e=''
alias f='find . -name'
alias g='grep -n'
alias gg='gedit'
alias h='history'
#alias i=''
alias j='jupyter notebook'
#alias k=''
alias ll='ls -alF'  # .bashrc default
alias la='ls -A'    # .bashrc Default
alias l='ls -CF'    # .bashrc Default
alias m='more'
alias n='nano'
#alias o=''
alias p='pwd'
alias pi='pip install -y'
#alias q=''
#alias r=''
alias s="find . -name '*.py'|xargs grep -i -n "  # Short for search
alias t='tree'
alias td='tree -d'
alias tdl='tree -d -L'
alias tl='tree -L'
#alias u=''
#alias v=''
#alias w=''
#alias x=''
#alias y=''
alias yi='sudo yum install -y'
#alias z=''

#################
# Alias: Docker #
#################
alias dr='docker run -it'
alias dp='docker ps'
alias da='docker attach'
alias ds='docker stop'
alias dc='docker commit'
alias dl='docker login'
alias dpu='docker push'

#========================#
source .bashrc_custom_path
#========================#
